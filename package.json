{
  "name": "bitbucket-people-manager",
  "version": "1.0.0",
  "description": "REST API to manage Bitbucket repository access",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun install && bun run build && bun dist/index.js",
    "dev": "bun install && bun --watch src/index.ts",
    "build": "bun install && bun build src/index.ts --outdir=dist --target=bun"
  },
  "dependencies": {
    "hono": "^4.6.8"
  },
  "devDependencies": {
    "@types/bun": "^1.2.17",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
